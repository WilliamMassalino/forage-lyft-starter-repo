# Import the CarFactory class from the car_factory module located in the same package.
# CarFactory is typically used to create instances of Car classes with specific configurations.
from .car_factory import CarFactory
